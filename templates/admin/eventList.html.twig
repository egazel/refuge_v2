<html>
    <head>
        <meta charset="UTF-8">
        <title>Liste des utilisateurs</title>
        <meta name="viewport" content="width=device-width, user-scalable=no">
        <link rel="stylesheet" href="/css/bootstrap.css">
        <link rel="stylesheet" href="/css/general.css">
        <link rel="stylesheet" href="/css/hover.css">
        <link rel="stylesheet" href="/css/admin.css">
        <script src="/js/jquery.min.js"></script>
        <script src="/js/popper.min.js"></script>
        <script src="/js/bootstrap.min.js"></script>
   
        <script src="https://kit.fontawesome.com/6576995845.js" crossorigin="anonymous"></script>

        {% include "partials/admin_header.html.twig" %}

    </head>
    <body>
        <h1 class="text-center mt-5">Liste des évenements</h1>
        <div class="container border shadow mt-5">
            <div class="row">
                <div class="col-sm mt-4">
                    <div class="text-center">
                        <button class="btn btn-warning m-*-auto" data-toggle="modal" data-target="#hostEventModalForm"><i class="fas fa-plus"></i> Organiser un événement </button>
                    </div>
                </div>
            </div>
            <div class="row mt-5">
                <div class="col-sm">
                    <table id="selectedColumn" class="table table-striped table-sm" cellspacing="0">
                    <thead>
                        <tr>
                        <th scope="col"><i class="fas fa-sort"> #</th>
                        <th scope="col"><i class="fas fa-sort"> Date</th>
                        <th scope="col"><i class="fas fa-sort"> Lieu</th>
                        </tr>
                    </thead>
                    <tbody>
                    {% for event in events %}
                        <tr>
                            <th scope="row">{{event.id }}</th>
                            <td>{{ event.date.format('d/m/Y - h:m') }}</td>
                            <td>{{ event.location }}</td>
                        </tr>
                    {% else %}
                        <tr>
                            <th> Aucun événement à venir !</th>
                            <td></td>
                            <td></td>
                        </tr>
                    {% endfor %}
                    </tbody>
                    </table>
                </div>
            </div>
        </div>
    </body>

    <div class="modal fade" id="hostEventModalForm" tabindex="-1" role="dialog" aria-labelledby="exampleModalCenterTitle" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="exampleModalLongTitle">Organiser un évenement</h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body">
                    {{ form_start(addEventForm) }}
                        <div class="form-group">
                        <label>Date et heure</label>
                            {{ form_errors(addEventForm.date) }}
                            {{ form_widget(addEventForm.date) }}
                        </div>

                        <div class="form-group">
                        <label>Lieu</label>
                            {{ form_errors(addEventForm.location) }}
                            {{ form_widget(addEventForm.location) }}
                        </div>

                        <div class="mt-3 text-center">
                            <button type="submit" class="btn btn-primary m-*-auto">Ajouter l'évenement</button>
                        </div>
                    {{ form_end(addEventForm) }}
                </div>
            </div>
        </div>
    </div>
</html>
<script>
    $(document).ready(function () {
        $('#selectedColumn').DataTable({
            "scrollY": "350px",
            "scrollCollapse": true,
        });
        $('.dataTables_length').addClass('bs-select');
    });
</script>
